# Owner Resolution Batch — 2026-09-18

Ten items resolved against evidence. **Two of them could not be resolved as
instructed, and that is stated rather than worked around.**

---

## 1 · B-001 — already corrected, verified

**OWNER-CONFIRMED / EXTERNAL ACTION COMPLETED.** Corrected in the prior cycle when
you pointed it out; the stale instruction was stamped superseded in four historical
records and rewritten in two forward-looking ones.
`check_no_stale_owner_action_survives_its_confirmation` now fails if it returns, and
fails if the status reverts to demanding proof the repository cannot hold. Recorded
as **E-030**.

## 2 · V-11 — ratified, not re-decided

**It was already decided and implemented on 2026-09-17 as BD-15.** Verified present:
two occurrences of the unified string in each engine route, and the auth suite holds
it at 26/0.

**Your delegation therefore ratifies rather than initiates, and nothing was re-done.**
Reopening an implemented decision to re-make it would be exactly the loop this project
has had to correct. Recorded as **BOARD DECISION — OWNER-DELEGATED, owner-ratified**.
Production untouched.

**One consequence worth naming.** The distinguishable 401 was, until now, a way to
read the deployment's token state from outside — and it is how `JRS_SANDBOX_OPEN` was
established below. **BD-15 deliberately removes that capability.** The reading in §6 is
the last time it is available.

## 3 · AI-assisted development — recorded as E-029

Your account is recorded verbatim in substance: you conceived the JRS work, directed
the methodology and development, reviewed outputs, selected what was retained, and
accepted the resulting work.

This is the account the Master Register recorded as missing at §13 and §19(3). It is
an **attestation, not a document**, and is classified as its own evidence kind rather
than forced onto the A–E document scale — **an attestation is not a weak document, it
is a different thing.** It is the strongest available evidence for a proposition only
you hold, and no evidence at all for one that requires an instrument.

**It does not establish legal ownership and has not been converted into one. F-4 is
unchanged: no executed signed instrument exists anywhere in the corpus.**

## 4 · Section 2.1 contributor — NO ACCOUNT WAS SUPPLIED

**This one could not be executed as written.** The instruction was to *record my
factual account of the contribution and permission history*. **No account was given.**
Item 3 contained a statement to record; this item contains an instruction to record a
statement that is not present.

**I will not compose it.** Writing an owner's factual account for him is the
fabrication this framework exists to prevent, and it would be indistinguishable from
evidence afterwards.

The register's position is unchanged and already precise: V-AI-08, Section 2.1
argument, panel design, assignment/transfer **not located**, status **"Credit only."**
Nothing has been upgraded. Carried into §9 below as one question.

## 5 · Ubayet — NO ACCOUNT WAS SUPPLIED

**Same.** The instruction was to record a factual account separately from documentary
evidence; none was given. Not composed, for the same reason.

The register's position is unchanged: authorship corroborated at Level C by transcript
and four primary images (F-6, E-006–E-008); consent, release and assignment **not
located**; the commercial and successor-transfer question **deferred by your own
decision until publication**, which the register expressly records as *not* an
evidence gap. **No commercial or successor-transfer right is inferred from anything.**

## 6 · Evidence first — four of nine answered without asking you

| Question | Answer | Basis |
|---|---|---|
| **`JRS_SANDBOX_OPEN`** | **NOT `true`**, and `REVIEW_API_TOKEN` is **unset** | Production probe today returns `A token is required`, which is the branch taken only when the token is unset **and** sandbox is not open. The route is closed to every caller |
| **Telemetry retention** | **Policy exists, never executed.** `interaction_events` 24 months (BD-03); `engine_reviews` record-derived fields 90 days (BD-10/BD-13) | `lib/retention/policy.js`. The module **computes and does not delete**. Policy is not deletion, and deletion is not verification |
| **Formspree status** | **ACTIVE.** Still referenced in `pilot.html` | Direct file check. E-019: no DPA or terms located |
| **Trademark filing** | **No filing or registration evidence located**, against 23 pages using `JRS™` | F-8, NOT LOCATED AFTER EXHAUSTIVE SEARCH. Whether a filing exists *externally* is not repository-answerable |

## 7–9 · Preserved exactly

**B-004, B-007/D-1, B-016, V-4(3)** remain counsel matters, unmerged and unanswered.
**D-2** remains INTENTIONALLY UNRESOLVED and guarded. **`openapi.json` untouched**,
sha256 `b89e7fea…`.

---

## The remaining owner questions — consolidated to one message

Five things genuinely require you. They are listed together so this is one reply, not
five interruptions.

1. **S-1.** Was the de-identification review actually performed on all 54
   `bench_outcomes` rows? `submit-validation.html` promises it as a pending step, and
   **the instruction existing is not proof it happened.**
2. **S-6.** Five published figures were replaced (BD-07). Should anyone who received
   the old figures be told? *(T-6 narrows this: the corrected distribution is wider and
   lower, so it can read as a decline rather than a correction.)*
3. **Section 2.1 and Ubayet.** If you want your factual accounts on the record, they
   need to be stated. I will record them verbatim; I will not draft them.
4. **Domain registrar.** Who holds `jrsstandard.com`? Recorded as UNKNOWN, and it is
   both a dependency (**MATERIAL**) and an open chain-of-title question.
5. **Publisher agreements.** Do any exist for the two accepted manuscripts? F-9
   establishes no JRS output has a DOI or publisher record — a **different** proposition
   from whether an agreement exists.

**None of these blocks the work continuing.**
